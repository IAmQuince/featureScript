# References

This folder contains sanitized reference notes for the recovered tributary design basis.

The original working document (`Tributaryv1.docx`) showed that the FeatureScript reached the section where tributaries needed to be generated, but the exact branch-building logic was still unresolved at that point. The package does not hide that history; the recovered notes are used to explain how the current implementation was repaired.

The physical-photo reference used during the 2026-05-16 update is intentionally not copied into this public/portfolio package. The code instead includes a generated preview and verification outputs.
